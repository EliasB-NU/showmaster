# Showmaster Client

This tool os for using the midi or gpio implementations. 

You control this tool via the command line, use `showmaster help` 
for more information

For automated start of the showmaster-client, you can use systemd.